{
  "builds": [
    { "src": "app.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "app.py" }
  ],
  "env": {
    "DATABASE_URL": "database.db",
    "FLASK_APP": "app.py",
    "FLASK_ENV": "production"
  }
}
